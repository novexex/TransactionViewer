
enum Constant {
    enum FileName {
        static let transactions = "transactions"
        static let rates = "rates"
    }
    
    enum FileExtension {
        static let plist = "plist"
    }
    
    enum ProductsConstant {
        static let title = "Products"
    }
}
